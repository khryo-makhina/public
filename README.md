This repository contains the `khryo-makhina` project as a Git submodule.

See [khryo-makhina/README.md](khryo-makhina/README.md) for the project’s canonical documentation.

To clone and initialize the submodule:
```bash
git clone --recurse-submodules <repo-url>
# or, after cloning:
git submodule update --init --recursive
```
